name: greetings test

on:
  push:
  branches: ["Himel"]

jobs:
  -name: greetings
  runs-on: ubuntu-latest
  steps:
    -name: hello world
    run: echo "Hello world, This is himel"




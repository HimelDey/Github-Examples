name: Node js CI

on:
  push:
  branches: ["Himel"]

jobs:
  name: Node js CI
  runs-on: ubuntu-latest
  steps:
    - name: Node Js Install
      uses: actions/setup-node@v6
      with:
        node-version: '18'
    
    - name: check node version
      run: node -v



name: custom-greeting-nodejs

on:
  push:
    branches: [Himel]

jobs:
  nodesjsApply:
    name: test nodejs
    runs-on: ubuntu-latest
    steps:
      - name: checknodejs
      - uses: actions/setup-node@v6
        with:
          node-version: '18'
      - run: npm install

